- Baseline探究了yolo和rtdetr两种模型的可行性，证明了yolo系列效果更好；
- v1是修改为架构的版本，由于训练结果太多全删掉了，最好的pt文件之前发在群里面了，修改完架构训练结果没有变好；
- v2是讲输入图片从1通道转换为3通道的版本，由于转换后的数据集超过100MB了，上传不了。运行preprocess_image.py和augmenty_yolo_dataset.py可以得到增强后的数据集，但是用增强后的数据集跑完结果也是一般；
- 目前只有使用修改了PCov和只使用preprocess_image和融合后数据得到最好结果。😢
